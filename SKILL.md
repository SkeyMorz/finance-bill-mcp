---
name: finance-bill-summary
description: 财务账单智能汇总 — 当用户需要汇总账单、生成财务报告、跨平台对账、或推送财务通知到飞书/企业微信/邮件时触发。自动扫描目录中的 .md 账单文件，解析多平台数据，生成 Markdown 汇总报告，可选跨平台对账，并推送到指定渠道。加载本 Skill 后禁止使用任何原生工具。
---

# 财务账单智能汇总

这是一个编排型 Skill。它不包含任何账单解析逻辑——所有解析、清洗、计算、对账、推送能力都由 MCP Tool 提供。Skill 只负责：**判断何时介入 → 按正确顺序调用 Tool → 质量决策 → 处理异常 → 呈现结果**。

---

## 铁律（硬约束，禁止违反）

加载本 Skill 后，以下规则对所有流程步骤均**严格强制**。任何绕过行为都会导致整个任务视为失败。

| # | 规则 | 说明 |
|---|------|------|
| 1 | **先读本文件，再碰任何工具** | Agent 看到匹配的触发条件后，第一件事就是完整读取 `SKILL.md`，锁定全部约束和流程，然后才能执行任何操作。 |
| 2 | **零原生工具** | 严禁调用 `shell_command` / `read` / `write` / `webfetch` / `websearch` / `grep` / `bash` / `osascript` 等 AI 环境自带的原生 I/O 工具。所有 I/O 必须通过 MCP Server 暴露的 Tool 完成。 |
| 3 | **严格按步骤执行** | 必须按 Step 1 → Step 2 → Step 3 → [Step 3.5 可选] → Step 4 → Step 5 → Step 6 的顺序执行，不得跳过步骤，不得调换顺序，不得合并步骤。 |
| 4 | **遇到异常不阻断流程** | 文件解析失败 / 个别工具返回空 / 推送失败 都不能中止后续步骤。记录异常，继续往下走，最终在 Step 6 统一报告。 |

**违反惩罚（用于自检）：**

- 如果使用了 `shell_command` 来浏览目录、读取文件、搜索配置 → **违反规则 2，本次流程无效，必须重来**。
- 如果没有先读取本文件就调用了第一个 Tool → **违反规则 1，本次流程无效，必须重来**。
- 如果跳过 Step 1 直接去读文件，或者跳过 Step 5 直接结束 → **违反规则 3，必须重来**。

**如何获取运行时信息而不违反铁律：**

| 需要 | 正确做法 |
|------|----------|
| 列出目录下的 .md 文件 | 调用 `finance-bill` MCP Server 的 `list_directory` Tool |
| 读取文件内容 | 调用 `read_bill_file` Tool |
| 查找 Webhook URL | 通过 `send_notification` 的 `target` 参数传入（URL 可由用户在对话中指定，或从 MCP Server 环境变量自动读取；禁止用原生工具搜索配置文件） |
| 查看项目结构 | 通过 `list_directory` 遍历各层目录 |

---

## 触发条件

Agent 的**第一动作**必须是读取本 SKILL.md 文件。读取之前不得调用任何 Tool。

当用户消息中包含以下意图时，立即触发本 Skill：

**精确触发：**
- "汇总账单" / "汇总 /data/finance/ 的账单"
- "生成财务报告" / "生成财报" / "月度汇总"
- "发送飞书" / "推送企业微信" / "发邮件" / "发到群里"
- "跨平台对账" / "对账" / "核对账单"
- "帮我做月度汇总"

**模糊触发（结合上下文判断）：**
- "看看这个月的收支情况"
- "把账单整理一下"
- "给老板发一份报告"
- "支付宝和银行流水对一下"

**不触发：**
- 用户只是询问账单格式、MCP 配置等技术问题
- 用户手动调用单个 Tool（如只读一个文件）
- 用户询问与财务账单无关的问题

触发后 Agent 必须立刻读取本 SKILL.md 全文，锁定铁律和流程后再开始执行。

---

## 执行流程（不可跳过、不可调换顺序）

本流程共 6 步（含可选子步骤），严格按顺序执行。

```
Step 1 → Step 2 → Step 3 → [Step 3.5] → Step 4 → Step 5 → Step 6
  │         │        │          │           │        │        │
  │         │        │     跨平台对账      │    通知(含重试)   │
  │         │        │     (可选,≥2平台)    │        │   最终展示
  │         │    质量决策点                  │        │
  │         │    (暂停/继续/跳过)            │        │
  │       逐文件读取                         │        │
  │         │                                │        │
  │     异常不阻断                            │        │
  │                                          │        │
 目录扫描                                     │        │
```

### Step 1 — `list_directory`

```
获取用户指定的目录下所有 .md 文件列表
```

- 如果用户未指定目录，询问用户提供路径
- 如果返回 `files: []`（无 .md 文件），**终止流程**并告知用户目录为空
- 将文件列表按名称排序后进入 Step 2

### Step 2 — `read_bill_file`（循环）

```
对 Step 1 返回的每个文件，逐一调用 read_bill_file 读取原始内容
```

- 逐个文件处理，不要并行跳过
- 如果某个文件读取失败（`isError: true`），记录该文件到**失败文件列表**，**继续处理下一个文件**
- 统计：成功 N 个，失败 X 个

### Step 3 — `parse_and_clean_bills`（循环 + 质量决策）

```
对 Step 2 成功读取的每个文件，调用 parse_and_clean_bills
传入 raw_content（Step 2 的 content）和 source_file（文件名）
```

得到每个文件的结构化数据：`{ platform, records, parse_warnings }`。

#### 3.1 质量分级决策（每个文件独立判断）

解析完成后，根据结果质量自动分级处理：

| 质量等级 | 判断条件 | 处理方式 |
|----------|----------|----------|
| 🟢 **正常** | `records.length > 0` 且 `parse_warnings.length == 0` | 直接进入 Step 4 |
| 🟡 **警告** | `records.length > 0` 且 `parse_warnings.length > 0` | 记录警告内容，**继续处理**；在 Step 6 中逐条展示警告 |
| 🟠 **低质量** | `records.length > 0` 且 `parse_warnings.length >= records.length * 0.5`（警告数超过记录数一半） | 标注 `⚠️ 低质量数据`，仍参与汇总；在 Step 6 显眼位置提醒用户人工复核该文件 |
| 🔴 **空记录** | `records.length == 0` | 保留该平台名称（`platform` 有效），标注 `⛔ 无有效记录`，**不阻断**后续步骤 |

#### 3.2 全局质量闸门

在进入 Step 4 之前，评估整体解析质量：

```
总记录数 = 所有文件 records 之和
总警告数 = 所有文件 parse_warnings 之和
空文件数 = records 为空的文件数
```

- 如果**所有文件**都是 🔴 空记录 → 生成一份空报告（收入/支出均为 0），告知用户「所有文件均无法解析，请检查文件格式」
- 如果总警告数 ≥ 总记录数 → 在 Step 4 报告标题中追加 `⚠️ 数据质量存疑，建议人工复核`

### Step 3.5 — `reconcile_platforms`（可选，≥2 个平台时强烈建议）

```
当 Step 3 成功解析了 2 个及以上平台的数据时，调用 reconcile_platforms
进行跨平台对账，发现匹配交易、单边记录和疑似重复
```

**触发条件：**
- Step 3 产生 ≥ 2 个有有效记录的平台
- 用户指令中包含"对账"/"核对"/"对比"/"cross-check" 关键词，或者各平台间存在明显重叠（如 alipay 和 bank_statement 同月数据）

**执行：**
- 选择记录数最多的两个平台作为对账双方
- 如果平台数 > 2，按交易量降序配对（如 alipay↔bank, alipay↔wechat）
- 调用 `reconcile_platforms`，传入各自的 records 和 platform 名称
- 对账结果附加到最终报告的「跨平台对账」章节

**对账结果解读：**
| 字段 | 含义 |
|------|------|
| `matched` | 同一笔交易在两个平台都有记录（含金额差异和日期偏差） |
| `unmatched_a` / `unmatched_b` | 仅在一个平台出现的记录——可能存在漏记 |
| `duplicates_a` / `duplicates_b` | 疑似重复记录——同一平台内重复录入 |
| `summary.match_rate` | 匹配率，< 50% 建议人工核查两平台数据完整性 |

### Step 4 — `generate_report`

```
将所有 Step 3 的结果合并为 bill_data 数组，调用 generate_report
传入 report_title 和 period
```

- `report_title`：根据目录名和当前日期自动生成，如质量闸门触发则追加 `⚠️`
  - 例：`"2026年3月财务汇总报告"` 或 `"2026年3月财务汇总报告 ⚠️ 数据质量存疑"`
- `period`：从目录名或文件名推断，如 `"2026-03"`
- 如果执行了 Step 3.5，将对账结果以 Markdown 表格形式追加到报告正文末尾

### Step 5 — `send_notification`（含重试 + 降级链）

```
用 Step 4 的报告文本作为 body，调用 send_notification
```

#### 5.1 渠道选择

| 用户指令关键词 | 选择渠道 |
|---------------|----------|
| "飞书" / "Lark" / 未明确指定 | `feishu`（默认） |
| "企业微信" / "微信群" | `wecom` |
| "邮件" / "邮箱" / "发到邮箱" | `email` |

#### 5.2 主渠道发送 + 重试策略

```
重试次数: 最多 2 次（共 3 次尝试）
重试间隔: 第 1 次重试等待 3 秒，第 2 次等待 6 秒（指数退避）
```

只在以下错误类型时重试：
- 网络超时（`timeout` / `ETIMEDOUT` / `ECONNRESET`）
- HTTP 5xx（服务器临时故障）

以下错误**不重试**，直接标记失败：
- HTTP 4xx（配置错误，如 401/403/404——重试没有意义）
- 缺少 URL / 参数错误
- DNS 解析失败

#### 5.3 降级链（Fallback Chain）

如果主渠道经过重试后仍然失败，按以下优先级自动降级：

```
飞书失败 → 企业微信 → Email → 保存到本地文件
```

降级规则：
1. 每次降级尝试同样有 2 次重试
2. 如果所有远程渠道都失败，将报告正文写入 MCP Server 所提供的**当前工作目录**下的 `report_output/` 文件夹
   - 文件名：`report_{period}_{timestamp}.md`
   - 在 Step 6 告知用户文件保存路径
3. **报告展示（Step 6）不受通知失败影响**——用户始终能看到报告内容

| 通知渠道 | Target 来源 |
|----------|------------|
| `feishu` | 用户指定 > `FEISHU_WEBHOOK_URL` 环境变量 |
| `wecom` | 用户指定 > `WECOM_WEBHOOK_URL` 环境变量 |
| `email` | 用户指定（收件人邮箱）；SMTP 配置从环境变量自动读取 |

### Step 6 — 向用户展示（分层输出）

根据数据质量和流程结果，按以下结构分层展示：

```
┌─────────────────────────────────────┐
│  📊 处理概况（一句话）                │
│  "处理 3 个文件，共 38 条记录"          │
├─────────────────────────────────────┤
│  📋 完整 Markdown 报告（Step 4 输出）  │
│  （含对账结果，如执行了 Step 3.5）      │
├─────────────────────────────────────┤
│  📢 通知状态                          │
│  "✅ 飞书发送成功" 或                  │
│  "⚠️ 飞书失败 → 企业微信成功" 或        │
│  "❌ 所有渠道失败，已保存至 report_output/" │
├─────────────────────────────────────┤
│  ⚠️ 异常明细（如有）                   │
│  - 失败文件列表 + 原因                 │
│  - 各文件解析警告汇总                  │
│  - 低质量数据文件标记                  │
│  - 对账异常（匹配率过低等）             │
└─────────────────────────────────────┘
```

---

## 错误处理策略（分层处置）

### 可恢复错误（记录 → 继续）

| 场景 | 处理方式 |
|------|----------|
| `list_directory` 返回空 | 告知用户该目录无 .md 文件，**停止流程**（不可恢复） |
| `read_bill_file` 对某文件失败 | 跳过该文件，记录到"失败文件列表"，继续处理其余文件 |
| `parse_and_clean_bills` 返回 `parse_warnings` | 警告不中断流程，按 §3.1 质量分级处理 |
| `parse_and_clean_bills` 返回 `records: []` | 保留该平台条目，标注"无有效记录"，继续 |
| 单个对账匹配率极低（< 20%） | 标注「两平台数据可能不重叠，建议人工核对」，不阻断 |
| `send_notification` 失败（可重试类型） | 按 §5.3 降级链依次尝试 |
| `send_notification` 失败（不可重试类型） | 直接进入降级链下一级 |

### 不可恢复错误（停止 → 报告）

| 场景 | 处理方式 |
|------|----------|
| 全部文件解析后 `records` 均为空 | 生成一份空报告（收入/支出为 0），告知用户「所有文件均无法解析，请检查文件格式是否为支持的 Markdown 表格 / CSV / 键值对」 |
| `generate_report` 调用本身失败 | 检查 bill_data 格式是否正确，将原始 parse 结果以纯文本形式展示给用户作为降级输出 |

---

## 输出规范

最终向用户展示的内容应包含：

1. **处理概况**（一句话总结：文件数、记录数、平台数）
2. **完整的 Markdown 报告**（Step 4 输出，如含对账则追加对账章节）
3. **通知状态**（成功 / 降级成功 / 失败 + 具体原因 + 降级路径）
4. **异常明细**（如有）：失败文件列表、解析警告汇总、低质量数据标记、对账异常

---

## 质量验收清单

交付给用户前，逐项自检：

- [ ] 全程未使用任何原生工具（`shell_command`、`read`、`write`、`grep` 等）
- [ ] 严格按照 Step 1 → 2 → 3 → [3.5] → 4 → 5 → 6 顺序执行
- [ ] Step 3 中每个文件都经过了质量分级判断
- [ ] 如 ≥ 2 个平台有数据，已执行或主动说明跳过 Step 3.5 的原因
- [ ] Step 5 失败时已执行重试 + 降级链
- [ ] 所有异常都已记录并在 Step 6 分层展示，没有静默吞掉
- [ ] 最终报告包含完整的处理概况 + 报告正文 + 通知状态 + 异常明细
